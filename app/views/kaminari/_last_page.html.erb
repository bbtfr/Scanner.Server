<% if current_page.last? %>
  <li class='disabled'><span><%= raw(t 'views.pagination.last') %></span></li>
<% else %>
  <li><%= link_to raw(t 'views.pagination.last'), url, :rel => 'last', :remote => remote %></li>
<% end %>
