<% if current_page.first? %>
  <li class='disabled'><span><%= raw(t 'views.pagination.first') %></span></li>
<% else %>
  <li><%= link_to raw(t 'views.pagination.first'), url, :rel => 'first', :remote => remote %></li>
<% end %>

